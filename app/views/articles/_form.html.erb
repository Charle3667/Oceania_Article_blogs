<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :title, class:'form-control', placeholder:'Article Title' %>
  </div>
  <br>
  <div>
    <%= form.text_area :body, class:'form-control', placeholder:'Article Title' %>
  </div>
  <br>
  <div>
    <%= form.number_field :user_id, id: :article_user_id ,value:current_user.id, type: :hidden %>
  </div>
  <div>
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
